<resources>

    <style name="Theme.PucAberta" parent="Theme.MaterialComponents.Light" />

    <style name="Theme.PucAberta.Fullscreen" parent="Theme.PucAberta">
        <item name="android:actionBarStyle">@style/Widget.Theme.PucAberta.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PucAberta.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>